# Koochi Development Notes

- Use `koochi --debug ...` for manual Koochi development and investigation runs so the debug analytics are available.
- For a single invariant, prefer `koochi --trace <test-id> --debug ...`.
- Do not commit generated `.koochi/debug/` artifacts.
