pub fn ignored_config_probe() {
    // KOOCHI_FAIL_IGNORED_DISCOVERY_CONFIG: this config should be ignored by --config.
}

pub fn override_config_probe() {
    // KOOCHI_FAIL_OVERRIDE_CONFIG_LIVE: the explicit --config file should run.
}
